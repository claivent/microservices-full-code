C:\_M\UU\development\JAVA\microservices-full-code>wsl --helpCopyright (c) Microsoft Corporation. Všechna práva vyhrazena.Informace o ochranì osobních údajù k tomuto produktu najdete na stránce https://aka.ms/privacy.Použití: wsl.exe [Argument] [Options...] [CommandLine]Argumenty pro spouštìní binárních souborù Linuxu:    Pokud není zadaný žádný pøíkazový øádek, spustí wsl.exe výchozí prostøedí.    --exec, -e <CommandLine>        Spustí zadaný pøíkaz bez použití výchozího linuxového prostøedí.    --shell-type <standard|login|none>        Spustí zadaný pøíkaz se zadaným typem prostøedí.    --        Pøedá zbývající pøíkazový øádek tak, jak je.Možnosti:    --cd <Directory>        Nastaví zadaný adresáø jako aktuální pracovní adresáø.        Pokud se použije ~, použije se domovská cesta uživatele Linuxu. Pokud cesta zaèíná        znakem /, bude interpretována jako absolutní cesta Linuxu.        V opaèném pøípadì musí být hodnota absolutní cestou systému Windows.    --distribution, -d <DistroName>        Spustí zadanou distribuci.    --distribution-id <DistroGuid>        Spustí ID zadané distribuce.    --user, -u <UserName>        Spustí jako zadaný uživatel.    --system        Spustí prostøedí pro distribuci systému.Argumenty pro správu Subsystému Windows pro Linux:    --help        Zobrazí informace o použití.    --debug-shell        Otevøe prostøedí ladìní WSL2 pro diagnostické úèely.    --install [Distro] [Options...]        Nainstaluje distribuci Subsystému Windows pro Linux.        Seznam platných distribucí zobrazíte pomocí pøíkazu 'wsl.exe --list --online'.        Možnosti:            --enable-wsl1                Povolí podporu WSL1.            --fixed-vhd                Vytvoøte disk s pevnou velikostí pro uložení distribuce.            --from-file <Path>                Nainstaluje distribuci z místního souboru.            --legacy                Použije starší verzi manifestu distribuce.            --location <Location>                Nastaví instalaèní cestu pro distribuci.            --name <Name>                Nastaví název distribuce.            --no-distribution                Nainstaluje pouze požadované volitelné souèásti, nenainstaluje distribuci.            --no-launch, -n                Po instalaci nespustí distribuci.            --version <Version>                Urèuje verzi, která se má použít pro novou distribuci.            --vhd-size <MemoryString>                Urèuje velikost disku pro uložení distribuce.            --web-download                Stáhne distribuci z internetu namísto z Microsoft Storu.    --manage <Distro> <Options...>        Zmìní specifické možnosti distribuce.        Možnosti:            --move <Location>                Pøesuòte distribuci do nového umístìní.            --set-sparse, -s <true|false>                Nastaví vhdx distribuce na zhuštìný, což umožní automatické uvolnìní místa na disku.            --set-default-user <Username>                Nastaví výchozího uživatele distribuce.            --resize <MemoryString>                Zmìní velikost disku distribuce na zadanou velikost.    --mount <Disk>        Propojí a pøipojí fyzický nebo virtuální disk ve všech distribucích WSL 2.        Možnosti:            --vhd                Urèuje, že <Disk> odkazuje na virtuální pevný disk.            --bare                Propojí disk s WSL2, ale nepøipojí ho.            --name <Name>                Pøipojí disk pomocí vlastního názvu pøípojného bodu.            --type <Type>                Systém souborù, který se má použít pøi pøipojování disku. Pokud není zadaný, výchozí hodnota je ext4.            --options <Options>                Další možnosti pøipojení.            --partition <Index>                Index oddílu, který se má pøipojit. Pokud není zadaný, výchozí hodnota je celý disk.    --set-default-version <Version>        Zmìní výchozí verzi instalace pro nové distribuce.    --shutdown        Okamžitì ukonèí všechny spuštìné distribuce a        odlehèený užitkový virtuální poèítaè WSL 2.    --status        Zobrazí stav Subsystému Windows pro Linux.    --unmount [Disk]        Odpojí disk a zruší jeho propojení u všech distribucí WSL2.        Pokud probìhlo volání bez argumentu, odpojí se všechny disky a zruší se jejich propojení.    --uninstall        Odinstaluje balíèek Subsystému Windows pro Linux z tohoto poèítaèe.    --update        Aktualizuje balíèek Subsystému Windows pro Linux.        Možnosti:            --pre-release                Stáhne pøedbìžnou verzi, pokud je k dispozici.    --version, -v        Zobrazí informace o verzi.Argumenty pro správu distribucí v Subsystému Windows pro Linux:    --export <Distro> <FileName> [Options]        Exportuje distribuci do souboru tar.        Název souboru mùže být - pro stdout.        Možnosti:            --format <Format>                Urèuje formát exportu. Podporované hodnoty: tar, tar.gz, vhd.    --import <Distro> <InstallLocation> <FileName> [Options]        Importuje zadaný soubor tar jako novou distribuci.        Název souboru mùže být - pro stdin.        Možnosti:            --version <Version>                Urèuje verzi, která se má použít pro novou distribuci.            --vhd                Urèuje, že zadaný soubor je soubor .vhdx, nikoli soubor tar.                Tato operace vytvoøí kopii souboru .vhdx v zadaném umístìní instalace.    --import-in-place <Distro> <FileName>        Importuje zadaný soubor .vhdx jako novou distribuci.        Tento virtuální pevný disk musí být naformátovaný typem systému souborù ext4.    --list, -l [Options]        Vypíše distribuce.        Možnosti:            --all                Vypíše všechny distribuce, vèetnì distribucí, které se                právì instalují nebo odinstalovávají.            --running                Vypíše pouze distribuce, které jsou aktuálnì spuštìné.            --quiet, -q                Zobrazí pouze názvy distribucí.            --verbose, -v                Zobrazí podrobné informace o všech distribucích.            --online, -o                Zobrazí seznam dostupných distribucí pro instalaci pomocí 'wsl.exe --install'.    --set-default, -s <Distro>        Nastaví distribuci jako výchozí.    --set-version <Distro> <Version>        Zmìní verzi zadané distribuce.    --terminate, -t <Distro>        Ukonèí zadanou distribuci.    --unregister <Distro>        Zruší registraci distribuce a odstraní koøenový systém souborù.