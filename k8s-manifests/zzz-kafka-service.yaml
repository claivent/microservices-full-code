apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: kafka
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
      nodePort: 30095

