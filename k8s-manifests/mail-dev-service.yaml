apiVersion: v1
kind: Service
metadata:
  name: mail-dev
spec:
  ports:
  - name: http
    port: 1080
    protocol: TCP
    targetPort: 1080
  - name: smtp
    port: 1025
    protocol: TCP
    targetPort: 1025
  selector:
    app: mail-dev
