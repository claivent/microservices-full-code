apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  #namespace: dev
spec:
  type: NodePort
  selector:
    app: kafka-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30094
