```bash
kubectl get po kiada-5876c89fc7-l66jm -o json | jq .status.conditions 
``` 

```bash
kubectl get po kiada-5876c89fc7-l66jm -o json > test.json

``` 

```bash
kubectl get po kiada-init -o json | jq .status

``` 

Whenever you want to create a pod manifest from scratch, you can also use  
the following command to create the file and then edit it to add more fields:  
```bash
kubectl run kiada --image=luksa/kiada:0.1 --dry-run=client -o yaml > mypod.yaml
``` 
. The --dry-run=client flag tells kubectl to output the  
definition instead of actually creating the object via the API.

```bash
 kubectl run clie --image=curlimages/curl -it --rm --restart=Never -- curl http://10.244.1.15:8080
``` 

## connect to pod from another pod

```bash
 kubectl run clie --image=curlimages/curl -it --rm --restart=Never -- curl http://10.244.1.15:8080
```

## LOGS

```bash
kubectl logs kiada
```

### Logs in real time
```bash
kubectl logs kiada -f
```
### Logs with timestamp
```bash
kubectl logs kiada --timestamps=true
```

```bash
kubectl logs kiada --since=2m
kubectl logs kiada --since-time=2020-02-01T09:50:00Z
```
### logs previous container add   --previous

```bash
kubectl logs kiada --tail=10
```

## dovniř kontaineru
```bash
kubectl exec kiada -- ps aux
```

```bash
kubectl exec -it kiada -- bash
```

```bash
kubectl exec -it kiada -- bash
```
###  Using kubectl attach to see what the application prints to standard output

```bash
kubectl attach kiada

```

```bash
kubectl attach -i kiada-stdin

```