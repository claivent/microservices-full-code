apiVersion: v1
kind: Service
metadata:
  name: product-service
spec:
  selector:
    app: product-service
  ports:
    - name: http
      protocol: TCP
      port: 8050
      targetPort: 8050
#      nodePort: 31050
#  type: NodePort
  type: ClusterIP
  