apiVersion: v1
kind: Service
metadata:
  name: customer-service
spec:
  selector:
    app: customer-service
  ports:
    - name: http
      nodePort: 30090
      protocol: TCP
      port: 8090
      targetPort: 8090
  type: NodePort
  