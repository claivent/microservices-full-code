apiVersion: v1
kind: Service
metadata:
  name: gateway-service

spec:
  selector:
    app: gateway-service

  ports:
    - name: http
      protocol: TCP
      port: 8222
      targetPort: 8222
  type: ClusterIP
