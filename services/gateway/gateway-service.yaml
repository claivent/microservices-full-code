apiVersion: v1
kind: Service
metadata:
  name: gateway-service

spec:
  selector:
    app: gateway-service

  ports:
    - name: http
      nodePort: 30092
      protocol: TCP
      port: 8222
      targetPort: 8222
  type: NodePort
