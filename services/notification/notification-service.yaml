apiVersion: v1
kind: Service
metadata:
  name: notification-service

spec:
  selector:
    app: notification-service

  ports:
    - name: http
      protocol: TCP
      port: 8040
      targetPort: 8040
  type: ClusterIP
